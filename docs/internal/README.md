# Internal process docs

Working notes for the builders. These files are **not** mirrored into `public/docs/`
and are not linked from the docs site index. Public product docs stay in `docs/`
(five reading-order sections, use cases, USER-GUIDE, TECH-GUIDE, SECURITY,
PHOTO-CREDITS).

`docs/internal/coordination/` holds the agent handoff logs and review records from the build
(append-only). It is tracked source, not mirrored into `public/docs`; see `PUBLIC-SCAN.md` for
what the history scan found and `coordination/PUBLISH-CHECKLIST.md` for the go-public order.
